name = "alejo-pulse"
main = "src/index.ts"
compatibility_date = "2024-09-01"

# Secrets (wrangler secret put …):
#   PULSE_API_KEY
#   PUSHER_APP_ID
#   PUSHER_KEY
#   PUSHER_SECRET
#   PUSHER_CLUSTER   (default mt1)

[vars]
# Public non-secret defaults
ALLOWED_ORIGINS = "http://localhost:5173,http://localhost:5174,https://localhost:5173,https://localhost:5174"
